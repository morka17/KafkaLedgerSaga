apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: saganova
  labels:
    app: order-service
    app.kubernetes.io/part-of: saganova
spec:
  replicas: 2
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
        - name: order-service
          image: saganova/order-service:latest # overridden per-environment - see infra/k8s/overlays/*/kustomization.yaml
          ports:
            - containerPort: 3001
          envFrom:
            - configMapRef:
                name: saganova-common-config
            - configMapRef:
                name: order-service-config
          env:
            - name: ORDER_SERVICE_PORT
              value: "3001"
            - name: DB_HOST
              value: "postgres"
            - name: DB_PORT
              value: "5432"
            - name: DB_NAME
              value: "saganova"
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef: { name: saganova-secrets, key: DB_USERNAME }
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef: { name: saganova-secrets, key: DB_PASSWORD }
          livenessProbe:
            httpGet: { path: /health/live, port: 3001 }
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet: { path: /health/ready, port: 3001 }
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits: { cpu: 500m, memory: 512Mi }
